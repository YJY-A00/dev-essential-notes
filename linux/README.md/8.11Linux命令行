linux学习（核心内容：查进程 / 内存 / 日志）
简单概要：用 grep 快速捞关键错误，用 awk 取特定列，用 vim 简单编辑。
                                             核心命令清单
   场景	                               命令	                                            说明
查所有进程	                          ps aux	                              显示所有用户的所有进程快照
查特定进程	                          ps aux | grep java	                  只显示包含“java”的进程
杀进程	                             kill -9 PID	                        强制终止某个进程（PID是进程号）
看内存概况	                          free -h	                            以人类可读方式显示内存总量、已用、可用
看实时资源	                          top	                           实时刷新 CPU/内存占用，按 q 退出，按 M 按内存排序
搜索日志关键词	                       grep "ERROR" app.log	                找出所有包含 ERROR 的行
显示关键词上下文	                    grep -C 3 "ERROR" app.log	           显示匹配行及前后各3行
实时滚动日志	                      tail -f app.log	                      像监控一样实时看新日志（Ctrl+C 停止）
提取日志某列	                      awk '{print $1, $4}' app.log	          取第1列和第4列（通常时间+信息）
打开编辑文件	                      vim app.log	                          用 vim 打开日志文件
vim 内搜索	                         按 /关键词 回车	                        查找关键词，按 n 跳下一个
vim 保存退出	                      按 Esc 输入 :wq 回车	                  保存并退出
vim 强制退出	                      按 Esc 输入 :q! 回车	                  不保存直接退出
-------------------------------------------------------------
在实操过程中首先摆在面前的是缺乏真正的 Linux 环境，经查询，可以打开电脑自带的Powershell下载 WSL（Windows 自带的 Linux 子系统）。好处：得到纯正的 Linux 黑
窗口装好 WSL 后，VS Code 会自动识别，可以直接在 VS Code 里打开终端（按 Ctrl + ``），默认就是 Ubuntu 环境，写笔记和敲命令无缝切换。
-------------------------------------------------------------
部分实操过程
1、下载WSL中：
图片：2026-08-12-10-20-45.png


2、查看系统状态（进程/内存/CPU）
图片：2026-08-12-11-30-15.png

  2.1 ps aux | head -10 —— 查看系统进程
输出解读：这张表列出了当前正在运行的前10个进程（系统程序）。
列名	       含义	                            意味着什么
USER	  谁在运行这个程序	          root（你）、systemd+（系统）、message+（系统服务）
PID	  进程ID（唯一编号）	       想杀掉某个程序时，就找这个号码
%CPU	  当前占用了多少CPU	          看到全是 0.0 和 0.2，说明系统很闲
%MEM	  占用了多少内存	             同样很小，说明没在跑重型任务
COMMAND	具体是什么程序	          /sbin/init（系统初始化）、cron（定时任务）等
核心结论：Ubuntu 现在很干净，没有跑什么大型应用，CPU和内存都闲着。


  2.2 free -h —— 查看内存使用情况
行	              含义	                                             你的数据
Mem	物理内存（电脑的真实内存条）	              总共 7.6GB，用了 590MB，剩余 7.1GB 可用（available）
Swap	虚拟内存（硬盘上划出来的，当内存不够时用）	总共 2GB，用了 0B（说明内存完全够用）
核心结论：电脑内存很充足，运行 Ubuntu 只占了不到 1GB，完全没问题。


  2.3 top -n 1 | head -10 —— 查看系统负载和CPU
  输出解读：top 是 Linux 的“任务管理器”，这里截取了它的头部信息。
  信息	               含义	                   数据解读
up 23 min	       系统已运行时间	        刚开机 23 分钟，很新
load average	   1/5/15分钟平均负载	     数值 0.17（远小于1），说明CPU非常空闲
Cpu(s): 99.6 id	CPU空闲率	              99.6% 是空闲的，几乎没有被占用
Tasks: 1 running	当前只有1个任务在运行	其他 21 个都在处于等待状态
核心结论：Ubuntu 现在非常健康，几乎没干活。在这上面跑什么命令都会非常流畅。


3.造一个日志文件（练 grep 和 awk）
图片：2026-08-12-15-14-35.png
  
命令	       符号	                  作用	                  结果
第1条        	> 	              创建/覆盖文件	         如果 app.log 不存在，就新建它；如果存在，就清空旧内容，写入这一行。
第2-4条	      >>               追加内容	               不会删掉已有的内容，而是把新行添加到文件末尾。

操作流程：
> 创建了 app.log 并写入了第一行（INFO 服务启动成功）。
>> 把第二行（ERROR 数据库超时）接在末尾。
>> 把第三行（INFO 登录成功）再接在末尾。
>> 把第四行（ERROR 支付接口返回500）再接在末尾。
INFO 是普通信息（系统启动、用户操作）
ERROR 是错误信息（连接超时、接口报500）

最后执行 cat app.log，就是把整个文件的内容打印到屏幕上，可以看到最终结果。


  3.1 用 grep 抓出所有 ERROR
   图片：2026-08-12-15-20-00.png

grep：是一个文本搜索工具（全称是 Global Regular Expression Print，全局正则表达式打印）。
"ERROR"：是要搜索的关键词。
app.log：是要搜索的目标文件。
  执行过程：grep 打开 app.log，从头到尾一行一行地读，把包含了 "ERROR" 这个关键词的行全部挑出来，打印在屏幕上。


  3.2 用 awk 只取时间和错误描述
   图片：2026-08-12-15-21-59.png

部分	               命令	                                     作用
左半	     grep "ERROR" app.log	               从 app.log 中筛出只包含 ERROR 的行（你上一步已经验证过）
管道                 |	                        把左边的输出结果，直接喂给右边的命令处理	相当于“接力”
右半	     awk '{print $1, $2, $4, $5, $6}'	   对接收到的每一行，按空格切分成若干“列”，然后只打印第1、2、4、5、6列

  去掉了中间的 ERROR 关键词，只保留时间和具体错误内容。


4.vim 练习
模式	               能干什么	                    怎么进入	                          怎么退出
普通模式（默认）	浏览文件、搜索、复制粘贴	  打开 vim 时默认就在这个模式	    按 i 进入编辑模式，按 : 进入命令模式
编辑模式	        像记事本一样打字修改内容	        按 i（插入）	              按 Esc 回到普通模式
命令模式	        保存、退出、搜索等操作	        按 :（冒号）	              按 Esc 可以退出命令模式回到普通模式
一句话总结：打开文件后，按 i 才能打字，按 Esc 退出来，按 : 输入命令保存退出。



  1）第一步：用 vim 打开 app.log（vim app.log）
   2026-08-12-16-31-06.png

  2）第二步：进入编辑模式，修改内容
  按 i 键（左下角会出现 -- INSERT -- 字样），表示现在可以打字了。
  用方向键把光标移到 数据库连接超时 这行，把 超时 改成 失败。
    2026-08-12-16-32-33.png
  改完后，按 Esc 键回到普通模式（左下角的 -- INSERT -- 消失）。
    2026-08-12-16-32-53.png

  3）第三步：保存并退出
  按 : 键（左下角出现一个冒号）。
  输入 wq（write & quit），按回车。
    2026-08-12-16-34-15.png
  文件保存并关闭，回到命令行。
    2026-08-12-16-34-41.png

  4）第四步：验证是否改成功
    2026-08-12-16-36-00.png
    可以看到，已由超时改成失败
                                          最常用的 6 个 vim 操作
操作	                                按键	                           说明
打开文件	                           vim 文件名	                     在命令行输入
进入编辑模式	                         i	                           在光标位置开始插入文字
退出编辑模式	                        Esc	                        回到普通模式
保存并退出	                        Esc + :wq + 回车	               在命令模式下输入 wq
不保存强制退出	                     Esc + :q! + 回车	               放弃所有修改，直接退出
搜索关键词	                        /关键词 + 回车	                  在普通模式下按 /，输入要搜的词，按回车跳转